import { createCatalogMetadata, createCatalogPage } from '@/lib/content/catalog-page-route'

export const generateMetadata = createCatalogMetadata('directions')
export default createCatalogPage('directions')

