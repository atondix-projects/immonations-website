/** Kurzschreibweisen für die Suchprofil-Datensätze. */
export const t = (de: string, en: string) => ({ de, en })

/** Stadtteil als Slug aus `DISTRICTS` plus Anzeigename. */
export const d = (slug: string, name: string) => ({ slug, name })
