# Public site URL. No trailing slash. Used in canonical URLs, sitemap entries, OG images.
# Set this to your real domain in production (e.g. https://immonations.example).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
