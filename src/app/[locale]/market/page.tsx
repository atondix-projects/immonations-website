import { createCatalogMetadata, createCatalogPage } from '@/lib/content/catalog-page-route'

export const generateMetadata = createCatalogMetadata('market')
export default createCatalogPage('market')
